<?php

function calc($calculate): string
{
    $first = '';
    $second = '';
    $arr1 = str_split($first);
    $arr2 = str_split($second);
    eval('+', '-', '(', ')');
    explode(string $first, string $second, int $limit = PHP_INT_MAX): array;
    return($calculate);
}

?>